---
// Simple 404 page for the Classic template
// Uses shared root public asset: /assets/falling.png
export const prerender = false;
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>404 — Page Not Found</title>
    <meta name="description" content="The page you requested could not be found." />
    <style>
      :root { font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, Apple Color Emoji, Segoe UI Emoji; }
      body { margin: 0; color: #e5e7eb; background: radial-gradient(1200px 800px at 50% -10%, #0b1220 0%, #0b0f1a 45%, #0a0a0a 100%); line-height: 1.6; }
      a { color: #10b981; text-decoration: none }
      .wrap { min-height: 100vh; display:grid; place-items:center; padding: 24px; }
      .card { text-align:center; max-width: 820px; }
      h1 { margin: 12px 0 4px; font-size: clamp(2rem, 5vw, 3rem); letter-spacing: -0.01em; }
      p { color:#94a3b8; margin: 10px 0; }
      .img { display:block; margin: 0 auto 12px; width: min(560px, 90vw); height: auto; filter: drop-shadow(0 10px 40px rgba(16,185,129,.25)); }
      .row { display:flex; gap: 10px; justify-content:center; flex-wrap:wrap; margin-top: 16px; }
      .btn { display:inline-block; padding: 10px 14px; border-radius: 999px; font-weight: 700; font-size: 14px; }
      .primary { background: #10b981; color: white; box-shadow: 0 6px 22px rgba(16,185,129,.35); }
      .secondary { color:#e5e7eb; border:1px solid rgba(148,163,184,.35); background-color: rgba(255,255,255,.06); }
      .tag { display:inline-block; padding:6px 12px; border-radius:999px; background: rgba(16,185,129,.12); color:#34d399; font-weight:600; font-size:12px; letter-spacing:.04em; border:1px solid rgba(16,185,129,.25); }
      footer { margin-top: 24px; color:#94a3b8; font-size: 13px; }
    </style>
  </head>
  <body>
    <main class="wrap">
      <section class="card">
        <img src="/assets/falling.png" alt="Lost astronaut falling" class="img" width="1120" height="700" loading="lazy" />
        <div class="tag">404 — Page Not Found</div>
        <h1>Looks like this page drifted off course.</h1>
        <p>
          The page you’re looking for doesn’t exist. But you can create a beautiful portfolio from your resume in minutes.
        </p>
        <div class="row">
          <a class="btn primary" href="/">Create a portfolio using your resume</a>
          <a class="btn secondary" href="/">Go back home</a>
        </div>
        <footer>
          Powered by <a href="/" rel="noopener">Foliomade</a> — turn your resume into a fast, shareable portfolio website.
        </footer>
      </section>
    </main>
  </body>
  </html>

